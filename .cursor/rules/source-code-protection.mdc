# Source Code Protection Rule

## Правило защиты исходного кода

**КРИТИЧЕСКОЕ ПРАВИЛО:** Все изменения исходного кода rtl_433 должны быть предварительно объяснены и одобрены пользователем.

### Что требует одобрения:
- Изменения файлов в `src/` (кроме custom файлов `rabbitmq_ook.c`, `rabbitmq_fsk.c`)
- Изменения файлов в `include/`
- Удаление любых файлов проекта
- Модификация `CMakeLists.txt`
- Изменения конфигурационных файлов

### Исключения (можно изменять без одобрения):
- Файлы в `client/src/` (наш custom код)
- Файлы в `shared/src/` (наш shared код)
- Файлы в `shared/include/` (наши заголовки)
- Custom декодеры `src/devices/rabbitmq_*.c`

### Процедура:
1. **ОБЪЯСНИТЬ** что нужно изменить и почему
2. **ДОЖДАТЬСЯ** одобрения пользователя
3. **ТОЛЬКО ТОГДА** делать изменения

### Формат запроса одобрения:
```
🔧 ЗАПРОС НА ИЗМЕНЕНИЕ ИСХОДНОГО КОДА:
- Файл: [путь к файлу]
- Причина: [зачем нужно изменение]
- Изменение: [что именно будет изменено]
- Альтернативы: [есть ли другие способы]

Требуется ваше одобрение для продолжения.
```

**НЕ НАРУШАЙТЕ ЭТО ПРАВИЛО!**